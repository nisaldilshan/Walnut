#include "FluidSolver2D.h"

FluidSolver2D::FluidSolver2D(uint32_t size)
{
}

FluidSolver2D::~FluidSolver2D()
{
}

void FluidSolver2D::FluidCubeAddDensity(int x, int y, int z, float amount)
{
    // int N = cube->size;
    // cube->density[IX(x, y, z)] += amount;
}

void FluidSolver2D::FluidCubeAddVelocity(int x, int y, int z, float amountX, float amountY, float amountZ)
{
    // int N = cube->size;
    // int index = IX(x, y, z);
    
    // cube->Vx[index] += amountX;
    // cube->Vy[index] += amountY;
    // cube->Vz[index] += amountZ;
}